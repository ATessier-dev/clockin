type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "Clockin",
  site_description: "Employee clock-in and scheduling for galerie l'original",
  site_domain: "https://schedule.loriginal.org",
};
